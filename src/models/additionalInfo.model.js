export default class additionalInfo {
  constructor(id, name, alias, globalId, platform, pathway) {
    this.id = id;
    this.name = name;
    this.alias = alias;
    this.globalId = globalId;
    this.platform = platform;
    this.pathway = pathway
  }
}
